node_modules/
dist/
.env
*.db
*.sqlite
.DS_Store
.venv/

# Kokoro TTS models (~300MB)
*.onnx
voices-*.bin

# Playwright CLI workspace
.playwright-cli/

# Claude Code worktrees
.claude/worktrees/
.worktrees/
.claude/

# Runtime data — real copies live OUTSIDE the repo (~/.claude/..., ~/.choomfie-hermes/).
# These are defense-in-depth in case a data dir is ever pointed inside the tree.
config.json
access.json
openai-api-keys.json
daemon-state.json
handoffs.json
*.port
inbox/
.choomfie-hermes/
.choomfie-data/
